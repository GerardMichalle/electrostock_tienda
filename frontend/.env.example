# Copia a .env.local y ajusta si hace falta. .env.local NO se sube a git.

# URL del backend (API Express).
# - dev:  http://localhost:4000
# - prod: la URL del backend desplegado (p. ej. https://api.electrostock.pe)
NEXT_PUBLIC_API_URL=http://localhost:4000
